[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Always: git lfs pull on checkout, then ensure full weights before docker build
buildCommand = "bash scripts/railway_lfs.sh"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
